package KotlinFundamental

fun main(){
    val name = "Damarazka"
    var school = "IDN"
    val shs = "Idn boarding school"

    print("Hello my name is ")
    println(name)
    println(if (true) "always true" else "always false")

    school = "International development networking"
    println(school)
}

// print() = just output the result
// println() = output will make a new line

// var = it's mutable so the value can be change
// val = it's immutable so the value can't be change